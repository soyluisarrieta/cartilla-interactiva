import { actionButtons } from './actionButtons.js'

// Establecer los botones de acción de ventana
actionButtons({
  minimizeId: 'minimize-window',
  maxResId: 'maximize-window',
  closeId: 'close-window'
})
